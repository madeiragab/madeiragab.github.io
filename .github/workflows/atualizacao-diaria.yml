name: Atualização diária do portfólio

# Todo dia às 12:34 (horário de Brasília = 15:34 UTC).
# Também pode ser disparado manualmente na aba Actions.
on:
  schedule:
    - cron: '34 15 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  atualizar:
    runs-on: ubuntu-latest
    steps:
      - name: Baixar o repositório
        uses: actions/checkout@v4

      - name: Atualizar dados (contribuições + projetos)
        run: node scripts/update-stats.mjs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_USER: madeiragab

      - name: Commitar e publicar a atualização
        # Autoria é do bot, não minha. Este commit é o Actions regravando dois
        # JSON sozinho — assiná-lo com o meu nome e o meu e-mail enchia o meu
        # gráfico de contribuições de atividade que eu não fiz, e ainda
        # empurrava o trabalho de verdade para baixo do ruído diário.
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "Nada mudou hoje — nenhum commit necessário."
          else
            git commit -m "chore(bot): regrava o snapshot de contribuições e projetos ($(date -u +%Y-%m-%d))"
            git push
          fi
